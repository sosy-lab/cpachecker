# This file is part of CPAchecker,
# a tool for configurable software verification:
# https://cpachecker.sosy-lab.org
#
# SPDX-FileCopyrightText: 2007-2020 Dirk Beyer <https://www.sosy-lab.org>
#
# SPDX-License-Identifier: Apache-2.0

# ----------------------------------------------------------------------
# This configuration file uses predicate analysis with ABE for
# abstractions at loop heads, combined with the ProgressBasedRefinementSelection,
# which monitors refinement progress via heuristics and terminates
# CEGAR early if all heuristics indicate divergence.
# ----------------------------------------------------------------------

#include includes/predicateAnalysis-PredAbsRefiner-ABEl.properties

#include includes/resource-limits.properties

specification = specification/default.spc

cpa.predicate.refinement.useProgressBasedRefinementSelection = true

cpa.predicate.refinement.progressBasedRefinementSelectionHeuristics.heuristicRefinerPairs=RUNREFINERNTIMES:STATIC,REACHED_SET_RATIO:DEFAULT,INTERPOLATION_RATE:DEFAULT,REDUNDANT_PREDICATES_PLATEAU:DEFAULT,STOP:STOP

cpa.predicate.progressBasedRefinementSelectionHeuristics.RunRefinerNTimes.numberRuns=2

cpa.predicate.progressBasedRefinementSelectionHeuristics.InterpolationRate.acceptableInterpolantRate=8.0

cpa.predicate.progressBasedRefinementSelectionHeuristics.ReachedSetRatio.abstractionLocationRefinementRatio=3.0

cpa.predicate.progressBasedRefinementSelectionHeuristics.ReachedSetRatio.refinementThreshold=10

cpa.predicate.progressBasedRefinementSelectionHeuristics.ReachedSetRatio.abstractionLocationThreshold=3

cegar.refiner=cpa.predicate.ProgressBasedRefinementSelection



