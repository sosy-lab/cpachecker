# This file is part of CPAchecker,
# a tool for configurable software verification:
# https://cpachecker.sosy-lab.org
#
# SPDX-FileCopyrightText: 2007-2020 Dirk Beyer <https://www.sosy-lab.org>
#
# SPDX-License-Identifier: Apache-2.0

# --------------------------------------------------------------------------
# This configuration runs the following 2 algorithms in parallel:
# 1. Interpolation-sequence Based Model Checking
# 2. Continuously-refined invariant generation using a data-flow analysis
#    over an interval-based abstract domain.
# The invariants generated by 2. are injected into the fixed point check of 1.
# The verdict returned by the invariant generator is ignored, i.e. the
# process continues even if it has proven the program safe.
# --------------------------------------------------------------------------
// this automaton defines which locations are the error locations
specification = specification/default.spc

#include includes/resource-limits.properties

#include bmc-interpolationSequence-dfInvariants.properties

parallelAlgorithm.configFiles = components/bmc-ABEl/bmc-itpseq.properties, components/invariantGeneration-no-out.properties::supply-reached-refinable:ignore-result
