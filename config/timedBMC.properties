# This file is part of CPAchecker,
# a tool for configurable software verification:
# https://cpachecker.sosy-lab.org
# 
# SPDX-FileCopyrightText: 2007-2020 Dirk Beyer <https://www.sosy-lab.org>
# 
# SPDX-License-Identifier: Apache-2.0

# disable bounding assertions (default is enabled)
# bmc.boundingAssertions = false

# The rest of the options should not be changed.

# best traversal order for formula creation
analysis.traversal.order = bfs

analysis.algorithm.BMC = true

# ARGCPA is needed in order to create an error path for unsafe programs
cpa = cpa.arg.ARGCPA
ARGCPA.cpa = cpa.composite.CompositeCPA

CompositeCPA.cpas = cpa.timedautomata.TAUnrollingCPA

# TA specific config
# ---------------------

# Disable unsupported outputs
cfa.export = true
cfa.exportPerFunction = false
cfa.callgraph.export = false
cpa.arg.export = false
counterexample.export.enabled = false
cpa.arg.export.code.header=false
cpa.arg.export.code.addAssumptions = false

language = CTA

# BMC config
analysis.useLoopStructure = false

cpa.timedautomata.initialMaximumStepCount = 1
cpa.timedautomata.stepCountAdujstmentStep = 1
cpa.timedautomata.stepCountUpperBound = 100

cpa.timedautomata.clockVariableType = RATIONAL

# Encoding 1
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = GLOBAL_ID
# cpa.timedautomata.timeEncoding = GLOBAL_IMPLICIT
# cpa.timedautomata.automatonEncodingType = CONSTRAINT_UNROLLING
# cpa.timedautomata.idleActionType = NONE
# cpa.timedautomata.delayActionType = GLOBAL
# cpa.timedautomata.actionDetachedDelay = false
# cpa.timedautomata.actionDetachedIdle = true
# cpa.timedautomata.invariantType = LOCAL
# cpa.timedautomata.allowZeroDelay = false

# Encoding 2
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = GLOBAL_ID
# cpa.timedautomata.timeEncoding = GLOBAL_EXPLICIT_DIFFERENCE
# cpa.timedautomata.automatonEncodingType = CONSTRAINT_UNROLLING
# cpa.timedautomata.idleActionType = NONE
# cpa.timedautomata.delayActionType = GLOBAL
# cpa.timedautomata.actionDetachedDelay = false
# cpa.timedautomata.actionDetachedIdle = true
# cpa.timedautomata.invariantType = LOCAL
# cpa.timedautomata.allowZeroDelay = false

# Encoding 3
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS, LOCAL_MUTEX_ACTIONS, UNSYNC_MUTEX_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = BOOLEAN_VAR
# cpa.timedautomata.timeEncoding = GLOBAL_EXPLICIT_DIFFERENCE
# cpa.timedautomata.automatonEncodingType = TRANSITION_UNROLLING
# cpa.timedautomata.idleActionType = NONE
# cpa.timedautomata.delayActionType = NONE
# cpa.timedautomata.actionDetachedDelay = true
# cpa.timedautomata.actionDetachedIdle = true
# cpa.timedautomata.invariantType = GLOBAL
# cpa.timedautomata.allowZeroDelay = false

# Encoding 4
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS, LOCAL_MUTEX_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = BOOLEAN_VAR
# cpa.timedautomata.timeEncoding = GLOBAL_EXPLICIT_DIFFERENCE
# cpa.timedautomata.automatonEncodingType = TRANSITION_UNROLLING
# cpa.timedautomata.idleActionType = NONE
# cpa.timedautomata.delayActionType = NONE
# cpa.timedautomata.actionDetachedDelay = true
# cpa.timedautomata.actionDetachedIdle = true
# cpa.timedautomata.invariantType = GLOBAL
# cpa.timedautomata.allowZeroDelay = false

# Encoding 5
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS, SHALLOW_SYNC_DIFFERENCE, ONLY_FINAL_IDLES, LOCAL_MUTEX_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = BOOLEAN_VAR_LOCAL
# cpa.timedautomata.timeEncoding = LOCAL_EXPLICIT_DIFFERENCE
# cpa.timedautomata.automatonEncodingType = CONSTRAINT_UNROLLING
# cpa.timedautomata.idleActionType = LOCAL
# cpa.timedautomata.delayActionType = LOCAL
# cpa.timedautomata.actionDetachedDelay = false
# cpa.timedautomata.actionDetachedIdle = false
# cpa.timedautomata.invariantType = GLOBAL
# cpa.timedautomata.allowZeroDelay = false

# Encoding 6
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS, SHALLOW_STRICT, LOCAL_MUTEX_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = BOOLEAN_VAR_LOCAL
# cpa.timedautomata.timeEncoding = GLOBAL_EXPLICIT_DIFFERENCE
# cpa.timedautomata.automatonEncodingType = CONSTRAINT_UNROLLING
# cpa.timedautomata.idleActionType = LOCAL
# cpa.timedautomata.delayActionType = LOCAL
# cpa.timedautomata.actionDetachedDelay = false
# cpa.timedautomata.actionDetachedIdle = false
# cpa.timedautomata.invariantType = GLOBAL
# cpa.timedautomata.allowZeroDelay = false

# Encoding 7
# cpa.timedautomata.encodingExtensions = INVARIANTS, TRANSITION_ACTIONS, SHALLOW_MULTISTEP, LOCAL_MUTEX_ACTIONS
# cpa.timedautomata.locationEncoding = LOCAL_ID
# cpa.timedautomata.actionEncoding = BOOLEAN_VAR_LOCAL
# cpa.timedautomata.timeEncoding = GLOBAL_EXPLICIT_DIFFERENCE
# cpa.timedautomata.automatonEncodingType = CONSTRAINT_UNROLLING
# cpa.timedautomata.idleActionType = LOCAL
# cpa.timedautomata.delayActionType = LOCAL
# cpa.timedautomata.actionDetachedDelay = false
# cpa.timedautomata.actionDetachedIdle = false
# cpa.timedautomata.invariantType = GLOBAL
# cpa.timedautomata.allowZeroDelay = false