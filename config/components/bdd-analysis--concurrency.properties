# This file is part of CPAchecker,
# a tool for configurable software verification:
# https://cpachecker.sosy-lab.org
#
# SPDX-FileCopyrightText: 2007-2025 Dirk Beyer <https://www.sosy-lab.org>
#
# SPDX-License-Identifier: Apache-2.0

# ************** SV-COMP'26 specifics ***********************

#include bddAnalysis-concurrency.properties

# This config can handle concurrency on its own, so we disable any type
# of preprocessing that would remove concurrency.
# Like for example the sequentialization preprocessing.
analysis.preprocessing.preferOriginalCfaOverSequentialized = true

# SEP allows us to produce precise counterexamples,
# which are better for witness validation.
cpa.bdd.merge = sep

cpa.callstack.skipVoidRecursion = true

analysis.checkCounterexamples=true
counterexample.checker=CPAchecker
counterexample.checker.config=../cex-checks/predicateAnalysis-concurrency-as-cex-check.properties
