AUTOMATON InteractionAutomatonA
LOCAL int x = 0;
INITIAL STATE Init;

STATE Init : 
  MATCH {st = $?; } -> DO x=x+1 PRINT "A: Matched st in line $line x=" PRINT x GOTO Init; 
  TRUE -> GOTO Init;