#include test_generation_single_variants.properties

tiger.fqlQuery = COVER "EDGES(ID)*".(EDGES(@LABEL(G1))+EDGES(@LABEL(G2)))."EDGES(ID)*"

tiger.numberOfTestGoalsPerRun = 2

cpa.automaton.dotExport = true
tiger.printARGperGoal = true