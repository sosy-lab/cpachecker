analysis.traversal.order = bfs
analysis.traversal.useTopsort = true

analysis.useAssumptionCollector=true
analysis.useAdjustableConditions=true

analysis.useRefinement = true

cegar.refiner = cpa.predicate.PredicateRefiner

cpa = cpa.art.ARTCPA
ARTCPA.cpa = cpa.composite.CompositeCPA
CompositeCPA.cpas = cpa.location.LocationCPA, cpa.callstack.CallstackCPA, cpa.assumptions.storage.AssumptionStorageCPA, cpa.assumptions.progressobserver.ProgressObserverCPA, cpa.predicate.PredicateCPA, cpa.conditions.globalconditions.GlobalConditionsCPA

cpa.conditions.global.time.wall = 60000
#cpa.conditions.global.time.wall.hardlimit = 180000

# this automaton defines which locations are the error locations
specification = config/specification/ErrorLocation.spc

cpa.predicate.blk.functions = true
cpa.predicate.blk.loops     = true
