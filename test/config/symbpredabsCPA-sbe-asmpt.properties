# only paths to error locations are analyzed
# REMOVE THIS IF YOU DON'T HAVE "Label: ERROR" IN YOUR PROGRAM!
cfa.removeIrrelevantForErrorLocations = true

analysis.useAssumptionCollector = true

analysis.useRefinement = true

cegar.refiner = cpa.predicate.PredicateRefiner

# ARTCPA is necessary for refinement and has to be the top-level CPA
cpa = cpa.art.ARTCPA

ARTCPA.cpa = cpa.composite.CompositeCPA

CompositeCPA.cpas = cpa.location.LocationCPA, cpa.callstack.CallstackCPA, cpa.predicate.PredicateCPA, cpa.assumptions.collector.AssumptionCollectorCPA, cpa.assumptions.genericassumptions.GenericAssumptionsCPA
#cpa.explicit.ExplicitCPA
#cpa.assumptions.progressobserver.ProgressObserverCPA

ProgressObserverCPA.cpa.progressobserver.heuristics = TimeOutHeuristics
#, RepetitionsInPathHeuristics, ReachedSizeHeuristics
TimeOutHeuristics.threshold = 1000
#3600000
RepetitionsInPathHeuristics.threshold = 3
ReachedSizeHeuristics.threshold = 1000

cpa.explicit.threshold = 20000000
cpa.explicit.merge = sep

cpa.monitor.limit = 0
cpa.monitor.pathcomputationlimit = 0
cpa.monitor.pathlengthlimit = 0
cpa.monitor.brancheslimit = 0

# this automaton defines which locations are the error locations
specification = test/config/automata/ErrorLocationAutomaton.txt

cpa.predicate.abstraction.cartesian = true
cpa.predicate.blk.threshold = 1
