# ------------------------------------------------------------------
# This configuration file uses restart analysis with two CPAs.
# The first analysis is explicit and it generates an assumption guiding automaton when
# it returns an unkown answer.
# The second algorithm is predicate analysis that uses the automaton to prune state space.
# ------------------------------------------------------------------

analysis.restartAfterUnknown = true

restartAlgorithm.configFiles = test/cmc/explicitAnalysis-cegar-generate-assumption.properties, test/cmc/predicateAnalysis-use-assumption.properties

