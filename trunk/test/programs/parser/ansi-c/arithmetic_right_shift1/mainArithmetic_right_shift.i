# 1 "arithmetic_right_shift1/main.c"
# 1 "<eingebaut>"
# 1 "<Kommandozeile>"
# 1 "arithmetic_right_shift1/main.c"





struct { char some[((-70)/16==-4)?1:-1]; };
struct { char some[((-70)>>4==-5)?1:-1]; };
struct { char some[((-16)>>1==-8)?1:-1]; };
struct { char some[((-1)>>1==-1)?1:-1]; };
struct { char some[(70>>4==4)?1:-1]; };

int main()
{
}
